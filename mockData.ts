
import { Asset } from './types';

// We export an empty array now to satisfy the user's request for manual input
// but keep the generation utility available for the modal to create initial sensor data.
export const INITIAL_ASSETS: Asset[] = [];
